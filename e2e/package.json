{
  "name": "forms-to-fabric-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E screenshot pipeline for Forms to Fabric documentation",
  "scripts": {
    "auth-setup": "npx tsx auth-setup.ts",
    "capture": "npx playwright test --project=capture",
    "capture:headed": "npx playwright test --project=capture --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0"
  }
}
